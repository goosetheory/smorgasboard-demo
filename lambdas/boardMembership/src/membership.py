class Membership:
	def __init__(self, board_membership_id, join_date, person_id, board_id, status):
		self.board_membership_id = board_membership_id
		self.join_date = join_date
		self.person_id = person_id
		self.board_id = board_id
		self.status = status
